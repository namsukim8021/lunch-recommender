# CLAUDE.md — lunch-recommender 상시 컨텍스트/규약

이 파일은 세션마다 AI에 **항상 주입**되는 프로젝트 규약이다(Harness의 **Constrain·Inform** 계층). 상세 명세는 `docs/`를 따른다.

## 프로젝트
회사(`서울특별시 성동구 아차산로13길 11`) 주변 점심 식당을 **랜덤 + 최근 안 겹치게** 추천하는 개인용 **정적**(무료) 웹. 데이터 = **Kakao 지도 JS SDK**.

## Harness 3계층 (본 레포)
| 계층 | 파일 | 하네스 축 |
|---|---|---|
| 상시 규약·컨텍스트 | **CLAUDE.md**(이 파일) | Constrain · Inform |
| 명세(무엇)·설계(어떻게)·원칙·**정답기준**·**판정방법** | `docs/spec.md`(SPEC) · `docs/plan.md`(LLD) · `docs/constitution.md` · `docs/oracle.md`(3층 오라클) · `docs/tracks.md`(검증 트랙 5종) | Inform |
| 절차 표준화(생성→검증→보완) | `.claude/skills/sdd-cycle` | Verify · Correct |

## SDD 운영 원칙 (반드시 준수)
1. **스펙이 1차 산출물.** 기능은 먼저 `docs/spec.md`(무엇)·`docs/plan.md`(어떻게)에 기술한 뒤 생성한다.
2. **코드를 직접 손보지 말 것.** 결함·변경은 코드가 아니라 **스펙(문서)을 보완한 뒤 재생성**한다(문서 보완 후 재생성 루프).
3. **정답기준 = 3층 오라클.** 생성물은 (회귀·재검증 기준으로) 사람 눈이 아니라 [`docs/oracle.md`](docs/oracle.md)의 **① 명세 오라클**(`spec §6` AC)·**② 도메인 오라클**(스펙에 없어도 당연한 불변식 D1~D10)·**③ 바이너리 오라클**(golden regression, 수용 후 생성) **대비로 판정**한다(최초 수용 1회는 사람 판단 허용 — oracle.md §2). 변경 시 "이 정답은 뭘로 정해졌나(golden/AC/도메인규칙)"를 먼저 답한다.
4. **판정 방법 = 검증 트랙.** 오라클이 "무엇이 정답인가"라면 [`docs/tracks.md`](docs/tracks.md)는 "그 정답과 **언제 무엇을 비교하는가**"다. 변경 착수 전 **어느 트랙으로 판정할지 먼저 정한다** — 트랙마다 선행 조건(결정성·계측)이 달라 구현 후에는 열 수 없다.
5. **파이프라인**(스펙 우선): SPEC(`spec.md`) → LLD(`plan.md`) → CODE → TEST → **VALIDATE(3층 오라클 × 트랙)**.

## 하드 제약 (constitution.md 요약)
- 정적·무료(GitHub Pages), **백엔드 없음**. 개인정보는 **localStorage만**, 서버 저장/계정 없음.
- 키는 **도메인 제한 Kakao JS 앱키만**. REST 키·시크릿은 레포/코드에 넣지 않는다.
- **창작 금지**: 없는 식당·메뉴·영업시간을 지어내지 않는다. 근사(거리 직선·점심영업 추정 등)는 "근사/추정"으로 **정직 표기**.
- 한국어 · 모바일 우선.

## 커밋 규칙
- AI 공동저자 트레일러는 정확히: `Co-Authored-By: Claude <noreply@anthropic.com>` (모델명·버전 등 변형 금지).
- 요청 프롬프트는 `PROMPTS.md`에 누적한다. **public 레포**이므로 사내 기밀·시크릿은 기록 금지.

## 현재 상태
Phase 0~4.6(스펙/설계/오라클/트랙) 문서화 완료 + **Phase 5(구현) 완료** — `config.js`/`lib/core.js`/`app.js`/`index.html` 생성, 난수원 주입(`pickRandom(list, rng)`)·계측(`elapsedMs`/`searchCalls`/`candidateCount`) 배선됨. 도메인 오라클(D1~D10) 자동 점검기 + 분포 판정기(D10) = `scripts/oracle-check.mjs`. **Phase 6(배포) 미착수** — GitHub Pages 미활성화, golden 스냅샷 미박제(바이너리 오라클은 여전히 콜드 스타트).

**3탭 확장** — 탭2 모락모락(구내식당 오늘의 메뉴, 브라우저 직접 fetch)·탭3 점심메뉴 월드컵(탭1 실제 후보 재사용 16강 토너먼트) 관련 문서(`docs/spec.md`·`docs/plan.md`·`docs/oracle.md`·`docs/tracks.md`·`docs/tasks.md` Phase 5.5) 갱신 완료(신규 AC·도메인 D12~D15 정의, [oracle.md](docs/oracle.md)·[tracks.md §3-3](docs/tracks.md)). **구현·검증 완료(오라클 D1~D15 17/17 PASS)** — `lib/core.js`/`app.js`(getter 노출 1줄)/`moremore.js`/`worldcup.js`/`tabs.js`/`index.html`/`config.js`/`scripts/oracle-check.mjs` 반영됨.

**모락모락 Actions 크롤러 전환(Phase 5.6) 완료** — 브라우저 직접 fetch가 CORS로 실제 차단됨을 실측 확인해, 아키텍처를 "브라우저 직접 fetch"에서 "**GitHub Actions 예약 크롤러**(`scripts/fetch-moremore.mjs`, `.github/workflows/moremore-fetch.yml` — 당초 평일 KST 09:00 1회, Phase 5.7에서 다중 슬롯으로 재조정됨)가 서버사이드로 수집해 `data/moremore-latest.json`(`{fetchedDate, raw}`)으로 커밋 → `moremore.js`가 같은 오리진 정적 파일을 fetch"로 전환했다. 진단용이었던 `.github/workflows/verify-moremore-fetch.yml`은 삭제. `lib/core.js`에 순수함수 `isFreshMoremoreData(fetchedDate, todayDate)` 추가로 날짜 불일치(4번째 경로)를 판정하며, `moremore.js`는 `ready && isFreshMoremoreData(...)` 모두 참일 때만 렌더한다(오라클 D12 4경로 확장). `config.js`에서 클라이언트가 더 이상 쓰지 않는 `MOREMORE_API_URL`/`MOREMORE_SRCH_OPER_CD`/`MOREMORE_SRCH_ASSIGN_CD`를 제거했고(값은 `scripts/fetch-moremore.mjs`에 상수로 이동), `data/moremore-latest.json`을 실데이터로 최초 커밋해뒀다.

**모락모락 예약 갱신 신뢰성 개선(Phase 5.7) 완료** — 배포 사이트에서 탭2가 "준비중입니다"만 뜨는 문제를 조사한 결과, GitHub Actions 예약(cron) 워크플로가 **best-effort**라 지연·드롭됨을 실측 확인했다(2026-08-26 예약분은 1시간 지연 발동, **2026-08-27 예약분은 발동 자체가 없었음**). `.github/workflows/moremore-fetch.yml`의 스케줄을 단일 슬롯(평일 KST 09:00 1회)에서 **평일 KST 07:13/08:29/09:41/10:07/11:23/12:37/13:51/15:17/17:33 9슬롯**으로 다중화했다(`workflow_dispatch` 유지) — 분(minute)까지 슬롯마다 흩은 이유는 드롭의 주원인이 정시 직후 큐 폭주라 같은 분에 몰리면 드롭이 상관돼 다중화 효과가 줄기 때문이고, 오후 슬롯(15:17/17:33)은 벤더가 당일 중 이미지·메뉴명을 점진적으로 채우는 것(실측: 이미지가 KST 11:12에 업로드됨)에 대응한다. 저장 게이트는 애초 "하루 1커밋" 판정으로 `shouldReplaceMoremoreData(existing, incoming, todayDate)`를 도입했다가, 워크플로 커밋 스텝의 `git diff --staged --quiet`가 바이트 동일 응답의 중복 커밋을 **이미** 막고 있어 그 판정이 불필요했고 오히려 부분 게시 결함(이른 슬롯이 1코너만 받아 커밋하면 뒤 슬롯의 확정 메뉴가 반영 안 됨)이 있음을 확인해, **`hasMoremoreItems(raw)`로 축소·개명**했다 — "응답에 저장할 항목이 최소 1개 있는가"만 판정하고, 있으면 코너 수와 무관하게 항상 반영한다(부분 게시도 정상 반영). 워크플로에 `concurrency`(group=moremore-fetch, cancel-in-progress=false)를 추가하고 push 직전 `git pull --rebase`를 넣어 슬롯 9개가 겹쳐 돌 때의 동시 실행/논-패스트포워드를 방어했다(단, 이 `git pull --rebase`가 충돌로 실패하면 그 슬롯 수집분은 유실되고 다음 슬롯이 메운다 — 재시도 루프 없음). 빈/스키마 깨진 응답이면 처음엔 슬롯과 무관하게 매번 `scripts/fetch-moremore.mjs`가 `exitCode=1`로 워크플로를 실패시켰으나, 적대적 리뷰에서 cron 9슬롯이 UTC `0-4`/`1-5` 조합으로 **KST 월~금에만** 발동해 주말엔 실행 자체가 없다는 점(따라서 "주말에도 붉게 뜬다"는 서술은 오류)과, 이른 슬롯의 빈 응답까지 매번 실패로 잡으면 경보 피로가 생긴다는 지적을 받아 **실패 판정을 당일 마지막 슬롯(KST 17:33)으로 한정**했다 — 워크플로가 환경변수 `MOREMORE_STRICT`를 그 슬롯일 때만 `1`로 주입하고, 신설한 `readStoredFetchedDate()`로 저장된 파일의 `fetchedDate`가 오늘이 아닐 때만(=하루가 끝났는데 오늘 데이터를 끝내 확보 못함) `exitCode=1`로 실패시킨다. 이로써 능동 알림 수단이 없는 상태의 유일한 탐지 신호는 유지하면서 **공휴일 오탐이 하루 최대 9건에서 1건으로** 줄었다. **클라이언트의 당일 정확 일치(freshness) 조건은 완화하지 않았다** — 어제 메뉴를 오늘 메뉴로 보여주는 것은 constitution의 창작 금지/정직 표기 위반이며, 갱신이 끝내 실패하면 종전대로 "준비중입니다"로 안내한다(주말은 cron 미실행이라 준비중 표시가 정상 동작). `scripts/oracle-check.mjs`의 D16이 `hasMoremoreItems` 기준으로 갱신됐다(`node scripts/oracle-check.mjs` 18/18 PASS). **미해결로 남은 것**: KST 자정 경계 근처 `workflow_dispatch` 수동 실행 시 요청 날짜와 응답 날짜를 대조하지 않아 어제 메뉴에 오늘 라벨이 붙어 커밋될 수 있는 경로(예약 슬롯 07:13~17:33에서는 발생하지 않으며, 저장 게이트가 "항목 1개 이상이면 항상 저장"만 판정해 이 경로에 방어막이 없음), 능동 알림 수단 부재(빨간 워크플로 외 없음), `git pull --rebase` 실패 시 슬롯 유실 — 전부 `docs/spec.md` §8에 명시. moremore.js/index.html의 이미지 카드 레이아웃(전면 사진·로드 실패 플레이스홀더)도 이 개선과 함께 바뀌었다.

**모락모락 카드 이미지 UI 개선(Phase 5.8) 완료** — "일부 테이크아웃 메뉴는 이미지가 보이는데 중식·석식은 안 보인다"는 지적을 조사한 결과, **이미지 부재의 원인은 파싱 버그·데이터 부재가 아니라 벤더(풀무원 모락모락)가 당일 중 이미지를 점진적으로 채우기 때문**임을 실측 확인했다(KST 10:11 수집분은 백반·스페셜 이미지가 `null`, 11:12 재조회 시 업로드됨 — 라면·석식처럼 코너에 따라 끝내 `null`로 남는 경우도 있음). `index.html`(`.mm-*` CSS)·`moremore.js`(`renderItems()`)를 카드 세로형(이미지 상단 full-bleed) 레이아웃으로 바꾸고, 실측 이미지 비율이 코너마다 제각각(1.36~1.71)이라 고정 크롭 대신 `.mm-photo{width:100%;height:auto}`로 원본 비율 그대로 렌더한다. 로드 전 레이아웃 점프는 `min-height` 스켈레톤으로, 사진 없음/로드 실패는 외부 이미지 없이 `.mm-media-ph`("사진 없음") 플레이스홀더로 폴백한다(창작 금지). `docs/spec.md`(§4 요구+데이터 특성, §6 AC)·`docs/plan.md`(카드 UI 절)·`docs/oracle.md`(AC 미러, 자동 오라클 부재를 정직 표기 — 이 판정은 도메인 D 규칙이 아니라 사용자 육안 요구에 대한 [oracle.md §2](docs/oracle.md) "최초 수용 1회는 사람 판단 허용" 예외)·`docs/tasks.md`(Phase 5.8)에 소급 반영했다.

**모락모락 적대적 리뷰 반영(Phase 5.9) 완료** — Phase 5.7~5.8 산출물에 대한 적대적 코드리뷰의 MAJOR/MINOR 지적을 커밋 직전에 반영했다. 서버측 실패신호 정밀화는 위 Phase 5.7 단락에 통합 기술. 클라이언트(`moremore.js`) 쪽: ① `fetch('data/moremore-latest.json', { cache: 'no-cache' })`로 캐시 재검증(벤더가 당일 중 갱신하는데 GitHub Pages 캐시 수명 동안 낡은 파일이 잡히면 갱신분이 사용자에게 도달하지 않는 문제 방지). ② 이미지 요청이 응답도 에러도 없이 매달리면 `onload`/`onerror`가 발화하지 않아 스켈레톤이 영구 잔존하는 결함을 발견해 `IMAGE_TIMEOUT_MS = 8000` 후 `img.complete`가 false인 사진에 `is-failed`를 붙여 플레이스홀더로 넘기도록 보완. ③ 플레이스홀더 문구를 "이미지 준비 전" → **"사진 없음"**으로 정정(`aria-label` 포함) — 라면(TAKEOUT)·석식처럼 끝내 이미지가 제공되지 않는 코너가 있어 "준비 전"은 오지 않을 사진을 약속하는 정직 표기 위반이었다. ④ `<img class="mm-photo">`의 `alt`를 `alt=""`(장식 이미지)로 변경 — 바로 아래 `.mm-name-ko`가 메뉴명을 텍스트로 이미 보여주므로 스크린리더 중복 낭독 방지. `node scripts/oracle-check.mjs` 18/18 PASS(회귀 없음), 크롤러 게이트 조합 검증 10/0·`renderItems` 마크업 검증 11/0(둘 다 1회성, `scripts/oracle-check.mjs`에 상시 편입 안 됨). `docs/spec.md`·`docs/plan.md`·`docs/oracle.md`·`docs/tasks.md`(Phase 5.9)·`PROMPTS.md`(표 병합 오류 정정)에 반영. 헤드리스 브라우저가 없어 실제 브라우저 렌더는 미검증.

**반경 800m·"내 위치" 버튼·Kakao 호출 절감(Phase 6.0) 완료** — 세 요청을 한 번에 반영했다. ① `config.js`의 `RADIUS`를 1000→800, `WALK_MINUTES`를 15→10으로 낮췄다("도보 15분≈1km" 직관에서 "도보 10분≈800m"로). `app.js`의 근사 상수 `WALK_METERS_PER_MIN`을 `67`(보행 4km/h)에서 `80`(보행 4.8km/h, 국내 "도보 1분=80m" 관행)으로 함께 바꿨다 — 67을 그대로 두면 `800m/67≈11.9분`이 반올림돼 "도보 약 12분"으로 표기돼 "800m=도보 10분" 요구와 어긋나므로, 정직 표기 원칙상 반경이 아니라 근사 상수 쪽을 조정했다. ② 탭1 버튼 줄에 `#my-location-btn`("📍 내 위치" ↔ "🏢 회사 기준" 토글)을 추가했다. `navigator.geolocation.getCurrentPosition`(`{enableHighAccuracy:false, timeout:10000, maximumAge:300000}`)으로 현재 위치를 받아 기준점(`center`)을 회사→내 위치로 바꿔 재수집·재추천한다. 실패(권한거부/확인불가/시간초과/미지원) 시 좌표를 지어내지 않고 회사 기준을 유지하며(`normalizeGeoPosition`/`describeGeolocationError` 신설, `lib/core.js`), 도움말 모달의 "회사에서/내 위치에서" 문구도 `originLabel(mode, addr)`로 실제 기준점을 따라간다(`window.__lunchTab1.originMode` getter로 탭3도 인지). ③ Kakao 호출 절감을 위해 `lib/places.js`를 신설해 SDK 로드·지오코딩·격자 검색을 탭1·탭3가 공유하게 했다(`worldcup.js`의 자체 재구현 전부 제거). 타일 결과를 `lunch_tiles_v1`(TTL 6시간·최대 120엔트리, 아래 보강에서 200→120으로 낮춤)에, 지오코딩 결과를 `lunch_geocode_v1`(TTL 30일)에 캐싱하고, 원 밖 타일은 검색 전에 프루닝(`isTileOutsideRadius`)하며, 같은 (center,radius) 동시 요청은 in-flight dedupe로 묶는다. 캐시에는 Kakao의 상대 `distance`를 저장하지 않고 좌표만 저장해, 병합 후 신규 `haversineMeters`로 **지금 기준점 기준**으로 다시 계산한다(기준점이 바뀌어도 옛 거리값이 새어나오지 않는다). **구현 중 처음 계획에 없던 결함을 발견해 대응했다** — 타일 격자가 기존처럼 center 상대로 앵커링되면 "내 위치" 버튼의 GPS 오차(클릭마다 좌표가 몇 m씩 흔들림)만으로 타일 경계가 통째로 달라져 캐시가 사실상 무용지물이 됐다. `buildGridTiles`에 4번째 인자 `{snap:boolean}`을 추가해(`snap:false`는 기존 동작과 바이트 단위로 동일 — D8 무영향) `snap:true`일 때 타일을 절대 격자(위도 0·경도 0 원점, 앵커 위도 `GRID_ANCHOR_DEG` 대역으로 경도 스케일 고정)에 정렬시켰다. `GRID_ANCHOR_DEG`는 처음 `0.1`로 뒀다가 앵커 대역 경계가 회사 좌표에서 북쪽 545m(도보 반경 800m **안**)에 걸려 재사용률이 0%로 떨어지는 문제를 실측해, `0.5`(경계가 북 22.8km/남 32.9km 밖으로 밀려남, `cos` 오차 0.28%)로 조정했다(회귀 테스트 D19b로 박제).

**적대적 코드리뷰로 실제 결함 2건을 발견해 수정했다** — 둘 다 기존 도메인 오라클이 그때까지 통과시키고 있었고, "반경 안 후보가 검색 자체에서 조용히 빠지는" 정보 손실이라는 같은 성격의 결함이었다. **(a) 프루닝 거리함수 불일치**: `isTileOutsideRadius`는 등장방형 근사로, 최종 `filterByRadius`는 하버사인으로 거리를 재서 서로 약 0.3% 어긋났다 — 실측 반례(`center={lat:37.54638305542474,lng:127.06547994871624}`, R=800)에서 최근접점 하버사인 799.70m(≤800m, 반경 안)인 타일이 프루닝으로 통째로 빠졌다. 프루닝 거리 함수를 하버사인으로 통일하고 `PRUNE_SAFETY_MARGIN_METERS=2`(항상 "덜 프루닝"하는 방향으로만 작동)를 추가했다. 전환 비용은 R=800/1200/1800 전 조합에서 +0~1타일로 거의 없었다. **(b) 스냅 격자 커버리지 구멍**: snap 모드가 원의 실제 크기(`radiusLng`)를 앵커 위도 스케일로 잘못 계산해, 앵커보다 북쪽 center에서 bbox가 안쪽으로 줄어드는 경우가 있었다 — 실측 반례(`center={lat:37.74457997,lng:126.90778388}`)의 정동 800m 지점(하버사인 799.10m)이 bbox 동단보다 2.38m 바깥이라 **어떤 타일에도 포함되지 않았다**. **⚠️ 이 결함 때문에 문서(plan.md/oracle.md)에 적어둔 "snap은 bbox를 바깥으로만 확장하므로 커버리지가 항상 보존된다"는 서술은 사실이 아니었고, 이번에 정정했다** — 원인은 원의 크기(`radiusLat`/`radiusLng`)를 격자 정렬용 앵커 스케일로 재계산한 것이었다. 수정: 원의 크기는 항상 `center.lat` 기준으로, 격자 정렬에만 앵커 스케일을 쓰도록 분리하고 스냅 전 bbox에 타일 1칸 여유를 추가했다 — 그 결과 원시 타일이 25→**49**로 늘었지만(여유 타일은 전부 원 밖이라 프루닝이 걷어냄) 실제 검색 대상은 **23으로 그대로**다(단, 프루닝 없이 `snap:true`만 쓰는 호출자가 생기면 타일 수가 2배가 되는 함정은 남는다).

같은 리뷰 라운드에서 신뢰성·프라이버시도 보강했다: `normalizeGeoPosition`의 반환 계약을 `{lat,lng,accuracy}`로 확장해 저정확도(데스크톱 WiFi/IP 측위 ±20km 등) 시 화면에 경고를 띄우고, 도움말 항목을 5→8개로 늘려 캐시 보관 시간·위치 재사용 시간·개인정보 미전송 고지를 추가했다. 헤더(`#app-subtitle`)와 월드컵 화면(`#worldcup-origin`)도 기준점을 추종하게 했고, 탭3가 수집 시점 기준점을 기억하지 않아 "내 위치"로 바꿔도 탭3는 계속 회사 기준으로 도는 죽은 코드(`originMode` getter 미사용) 결함도 고쳤다. **설계 결정 뒤집힘**: 최초엔 "이력 초기화" 버튼이 `clearTileCache()`를 의도적으로 안 불렀다(캐시까지 지우면 호출 절감이 무력화된다는 논리). 그런데 타일 캐시 키가 절대 좌표라 사용자가 있던 위치가 최대 6시간 삭제 수단 없이 남는 프라이버시 결함이 지적돼, `handleResetHistory`에 `clearTileCache()`를 연결하고 버튼 라벨을 **"이력·캐시 초기화"**로 바꿨다 — 캐시 삭제는 사용자가 명시적으로 누를 때만 일어나므로 절감 효과는 유지된다. 그 밖에 SDK 싱글턴이 `services` 준비 전에 잘못 resolve되던 결함(탭3가 새로고침 전까지 영구 사망), `Promise.all`→`Promise.allSettled`(타일 1개 실패로 성공분 전체 폐기 방지), in-flight dedupe 키 반올림, 캐시 write를 read-modify-write로, `categorySearch`의 `location` 파라미터 제거, 캐시 엔트리 캡 200→120도 함께 고쳤다.

실측(회사 좌표 근사값 `{lat:37.5451, lng:127.0554}` 기준): 첫 방문(콜드) 검색 대상 타일은 `snap:false`+프루닝만 18개(최대 54회) vs 실제 경로 `snap:true`+프루닝 23개(최대 69회) — **절대 격자 정렬이 콜드 호출 수를 오히려 늘린다(+27.8%)**. 캐시 재사용률(24방향 평균, 결함 수정 반영): 5m 100.0%, 100m 98.4%, 300m 84.6%, 600m 67.1%, 800m 54.7%(모두 `snap:false`는 0.0%). 모의 SDK 종단 시뮬레이션: 같은 기준점 재방문은 0회, 137m 이동 재수집은 16회→2회. 커버리지 몬테카를로는 **처음엔 좌표당 표본 2,000점으로 위 결함(b) 반례를 놓쳤다**(정직하게 남긴다) — 표본을 좌표당 20,000점(총 약 500만점)으로 올리고 실측 반례 2곳을 회귀 케이스로 고정한 뒤 재확인했다.

**2차 적대적 리뷰 반영(코드 확정) 완료** — Phase 6.0 산출물을 한 번 더 리뷰해 새 결함·개선을 반영했고, 이 라운드를 끝으로 코드는 확정됐다(`docs/tasks.md` Phase 6.0c). ① **초기화 경합**: 검색 진행 중에 "이력·캐시 초기화"를 눌러도, 수집 완료 시점의 read-modify-write가 방금 지운 엔트리를 되살리는 결함이 실측됐다(초기화 후 23개 재기록). `setBusy()`가 초기화 버튼도 함께 잠그고, `lib/places.js`에 **캐시 세대 카운터**(`clearTileCache()`가 증가시킴)를 둬 수집 시작 시점과 세대가 다르면 그 수집분 저장을 건너뛰도록 2중으로 막았다(버튼 밖 경로·동시 수집까지 커버) — 수정 후 잔존 엔트리 0개. ② **캐시 캡이 소프트 캡이 됨(중요)**: 캡(120)이 단일 수집의 타일 수보다 작아지는 반경(예: R=2700→172타일) 구간이 있어, 수집 직후 자기 타일이 밀려나 곧바로 콜드 재수집(`fetchedTiles=52`)이 나는 역전 현상이 실측됐다. `evictOldestTiles(cacheMap, maxEntries, protectedKeys=[])`에 **3번째 인자(보호할 키 목록)**를 추가해 이번 수집이 실제로 쓴 타일은 상한 계산에서 보호한다(미지정 시 기존 동작 그대로라 D25 무영향; 동률 `ts`는 나중에 삽입된 키를 남기는 tie-break 포함) — 수정 후 R=2700 재수집은 `fetchedTiles=0`/`cachedTiles=172`. **실효 상한은 `max(120, 단일 수집 타일 수)`인 소프트 캡**이라, 그 구간에서 캐시가 일시적으로 약 172엔트리(추정 2.9MB, 5MB 한도 대비 여유가 가장 얇음)까지 부풀 수 있음을 §8에 남겼다(반경 상한·필드 축소는 이번 범위 밖). ③ **탭3 재수집 규칙 정교화**: 기준점 서명에 `radius`를 포함하고(전엔 mode+좌표만 봐서 탭1에서 반경을 넓혀도 탭3가 옛 반경 풀을 유지), 수집 중 들어온 탭 전환을 `pending` 플래그로 보관했다가 완료 직후 서명을 재확인해 필요하면 재수집하며(전엔 이벤트가 소실돼 탭을 한 번 더 왕복해야 갱신됨), 재수집 조건을 `current.radius > collected.radius`일 때로 한정(탭3에서 넓힌 반경을 탭 전환이 되돌리지 않도록), 탭3 자체 수집 경로의 반경 소스를 `CONFIG.RADIUS`에서 `__lunchTab1.radius || CONFIG.RADIUS`로 바꿨다(안 그러면 서명과 수집 반경이 어긋나 탭 전환마다 재수집이 반복되는 것을 실측으로 발견). 검증: 탭1 800→1200 후 탭3 복귀 시 표기 "800m→1200m", SDK 호출 23→42(캐시 23 재사용, 19만 신규); 조건 변화 없이 탭 4회 왕복해도 호출 23→23. ④ **탭3 반경 확대 더블클릭**: 연타 시 표기가 실제 풀(1200m)이 아닌 의도 반경(1800m)을 보여주는 정직 표기 위반과, 늦게 끝난 수집이 진행 중 토너먼트를 리셋하는 문제가 있어 수집 중 버튼을 잠그고 표기는 실제 풀을 만든 `collectedOrigin.radius`를 쓴다. ⑤ **저정확도 경고를 상시 배지로 분리**: 상태줄에 쓰면 검색 실패 메시지를 덮어쓰고 "다른 곳"을 누르면 사라지는 1회성이라, 전용 `#geo-accuracy-notice` 배지로 옮겨 `originMode==='geo'`이고 `accuracy>currentRadius`인 동안 계속 표시하고 회사 기준으로 돌아가면 사라지게 했다 — **위 미해결 목록의 "저정확도 경고 1회성" 항목은 이걸로 해결됐다.** ⑥ **접근성 정정**: 1차 리뷰에서 넣은 `aria-pressed`가 라벨 토글("🏢 회사 기준")과 겹쳐 내 위치 활성 상태에서 "회사 기준 버튼, 눌림"으로 낭독되는(실제와 정반대) 문제가 있었다 — WAI-ARIA APG는 토글 버튼의 접근 가능한 이름을 상태로 바꾸지 말라고 명시한다. 라벨을 `📍 내 위치`로 고정하고 `aria-pressed`+`.is-active`(+헤더 문구)로만 상태를 전달하도록 고쳤다. ⑦ **삭제 범위·문구 정합**: `handleResetHistory`가 `lunch_metrics`(계측값도 사용자 데이터)까지 `clearMetrics()`로 지우고, `lunch_geocode_v1`(회사 주소 좌표, 개인 위치 아님)은 유지하되 그 예외를 도움말 문구에 명시했다. ⑧ **데드 코드 제거**: `worldcup.js`의 미사용 `escapeHtml`(전부 `textContent` 사용이라 이스케이프 불필요), `app.js`의 미사용 `geoCenter`, 즉시 덮이던 상태 문구, `#worldcup-origin`은 비었을 때 `hidden`.

**오라클 확장(D29~D31 + D17 픽스처 보강)**: 기존 D22~D28은 Geocoder 스텁이 항상 ZERO_RESULT라 `resolveCompanyCenter`의 성공 경로·`lunch_geocode_v1` 캐시가 한 줄도 실행되지 않고 있었다는 걸 2차 리뷰가 지적해, **D29**(지오코딩 캐시 콜드/웜/TTL만료/시계뒤틀림(미래 ts)/`config.CENTER` 우선/실패 시 캐시 미기록)를 추가했다. **D30**은 다중 페이지(정확히 2회)·`ZERO_RESULT` 타일(1회, 빈 배열 캐시)·`MAX_PAGES_PER_TILE=3` 상한(정확히 3회에서 멈춤)·`QuotaExceededError`(throw 없이 수집 성공 + 캐시 미기록)를 검증한다. **D31**은 `GRID_ANCHOR_DEG` 대역 경계를 사이에 둔 두 좌표의 캐시 키 교집합이 **정확히 0**임을 의도적으로 박제한다 — 실패가 아니라 이 대역 분할 설계의 알려진 특성이며, 나중에 `GRID_ANCHOR_DEG`를 만질 때 영향 범위가 드러나게 하려는 회귀다. **D17 픽스처도 보강됐다**: 기존 반례 하나만으로는 "거리 함수를 haversine에서 등장방형으로 되돌리되 마진(2m)은 남긴" 유형의 회귀를 놓친다(그 타일 최근접점이 등장방형 800.613m라 800+2 미만이라 통과해버림) — 그래서 그런 유형을 잡는 조합을 국내 좌표대(위도 33~43N×경도 126~129E)×반경(800~8000m)에서 격자 탐색했고, **실사용 반경(800/1200/1800m)에서는 해당 조합이 0건**이었다(500건을 찾았지만 전부 반경 2500m 이상) — 최종 채택한 회귀 케이스는 `center={lat:35,lng:129}, radius=8000`(haversine 7999.89m ≤ 8000, 등장방형 8010.11m > 8002)이며, 이는 실사용 반경에서는 이 유형의 회귀가 발생하지 않는다는 뜻이기도 하다. 스텁도 `QuotaExceededError`를 던지는 localStorage와 `parentNode`를 가진 document를 갖추도록 보강해, SDK 로드 실패 시 script 정리+재시도 경로(D27)까지 실제로 검증되게 했다. D24의 `TILE_CACHE_TTL_MS===21600000` 하드코딩 단언은 TTL이 사용자 체감 동작이라 조용히 바뀌면 안 된다는 의도된 박제임을 주석으로 남겼다.

**최종 확정치**(2회 실행 stdout 동일 = 결정적): `node scripts/oracle-check.mjs` → **`TOTAL: 34 passed, 0 failed, 0 skipped`**(D1~D16 18건 + D17~D24 8건 + D19b 1건 + D25~D31 7건). `docs/spec.md`(§3 UC4/UC5/UC8, §4, §6 AC, §8 미해결 갱신 — "저정확도 경고 1회성" 해결로 정정 + 소프트 캡·pending 실패 잔존 등 신규)·`docs/plan.md`(정정 2건 + B-1~B-8 반영)·`docs/oracle.md`(D29~D31 신설, D17 픽스처 보강 기록)·`docs/tracks.md`·`docs/tasks.md`(Phase 6.0c)·`README.md`·`PROMPTS.md`에 반영했다. **미해결로 남은 것(최종)**: 콜드 방문 호출 순증(+27.8%), 앵커 대역 경계를 사이에 둔 두 기준점은 캐시 미공유(D31이 박제, 경계 간격 넓어 실사용 영향 낮음), `GRID_ANCHOR_DEG`를 다시 줄이면 캐시 무효화 퇴행 재발, `PRUNE_SAFETY_MARGIN_METERS`를 0 이하로 낮추면 프루닝 유실 재발 위험, 프루닝 없이 `snap:true`만 쓰는 호출자가 생기면 타일 수 2배, 소프트 캡 구간(R=2700 직후)에서 캐시가 일시적으로 약 2.9MB까지 부풀 수 있음, 타일 캐시 TTL(6시간) 동안 신규 개업/폐업 미반영, geolocation은 HTTPS/localhost 전용(`file://` 미동작), `accuracy` 음수는 걸러지지 않음(실질 위험은 낮음), `pending` 플래그가 수집 실패 경로에서 남지만 다음 activate에서 정상 처리(무한 재수집 없음), 45개 상한에 걸리는 밀집 타일은 `location` 파라미터 제거로 어느 45개가 오는지 달라질 수 있고 `isPageTruncated`는 `console.warn`만 하고 화면 고지가 없음(자동 오라클 없음), 실기기 위치 권한 프롬프트·`aria-pressed` 낭독·배지 표시·버튼 비활성은 헤드리스 환경 부재로 스텁 수준까지만 검증(실기기 확인은 사람 몫) — 전부 `docs/spec.md` §8에 명시.
